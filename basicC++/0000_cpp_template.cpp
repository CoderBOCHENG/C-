#include <stdio.h>
#include <math.h>
#include <limits.h>
#include <float.h>
#include <string.h>

#include <vector>
using namespace std;


int main()
{


    printf("Hello C/C++!"); // f means 'formatted'




    return 0;
}

/*
IMPORTANCE!!! -----------------------------------------------
1) stdio.h means: STandarD Input / Output Header file.
2) #include <stdio.h> mean: import the header file. It allows you to do print and input

3) int main() define a function.
   int       means: the function returns a int value.
   main()    means: the function name
   {}        means: the function body
   return 0; means: I have to return a int value, so I return 0.

4) Python runs the program from 1s line;
   C/C++ runs the program from main function.   

5) all lines end with ;

6) This is the template which you copy whenver you start coding a new C/C++ program.
-------------------------------------------------------------
*/