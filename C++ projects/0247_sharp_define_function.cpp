#include <bits/stdc++.h>
using namespace std;

#define SQUARE(x) ((x) * (x))
#define MAX(a, b) (((a) > (b)) ? (a) : (b))



int main() {


    // cout << "Enter a interger: ";
    // int num = 0;
    // cin >> num;

    // cout << SQUARE(num) << endl;

    cout << MAX(25, 101) << endl;
    cout << MAX(0.1, 0.2) << endl;

    cout << SQUARE(2 + 2) << endl;
    // the above line will be explained as 2 + 2 * 2 + 2


    return 0;
}

/*
1) They accept different parameter type - int / double / long long 


*/